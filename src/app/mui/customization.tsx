"use client"
import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#5f7a8d",
      dark: "#3a4852",
      contrastText:'#fff',


    },
    secondary: {
      main: "#fdc040",
    },
  },
});
